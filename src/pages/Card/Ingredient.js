

export const Ingredient = (props) => {
	return (
		<>
			<div>{props.name}</div>			
		</>
		)
}

export const Amount = (props) => {
	return (
		<>
			<div>{props.anount}</div>			
		</>
		)
}